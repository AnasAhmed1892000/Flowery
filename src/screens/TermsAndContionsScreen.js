import { SafeAreaView, StyleSheet, Text, View } from "react-native";
import React from "react";

const TermsAndContions = () => {
  return (
    <View>
      <SafeAreaView>
        <Text>termsAndContionsScreen</Text>
      </SafeAreaView>
    </View>
  );
};

export default TermsAndContions;

const styles = StyleSheet.create({});
